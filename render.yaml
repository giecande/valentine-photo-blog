services:
  - type: web
    name: valentine-photo-blog
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ADMIN_PASSWORD
        generateValue: true
      - key: NODE_ENV
        value: production
